public class FileAvecPrio<T extends Comparable<T>> implements IFileAvecPrio<T>{
    protected ArrayLIst<T> al;
    /*public boolean remove()
    test si vide
    T t=maxPtio;
    return al.remove(t);*/

    /*T maxPrioritaire(){
        if(IsEmpty)throw new exception...

    }

    /*iterateur ordre naturel
    public Iterateur<T> iterator(){
        return al.iterator;
    }
    */

    /*
    FileAvecPrio<Integer>
    fap=new FileAvecPrio<>();
    
    */
}